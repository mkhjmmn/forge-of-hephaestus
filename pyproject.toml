[project]
name = "theforgehephaestus"
version = "0.1.0"
description = "A lightweight Flask backend service for The Forge of Hephaestus."
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]
